server:
  port: 8083

spring:
  application:
    name: applicationservice
  cloud:
    consul:
      discovery:
        hostname: localhost
      config:
        enabled: true
        prefix: config
        defaultContext: application
        profile-separator: ':'
        format: YAML
